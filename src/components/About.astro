---
---

<section class="about">
	<div class="container">
		<h2 class="about-heading">Sobre nosotros</h2>
		<p class="about-text">
			Rumba Corp es el ecosistema salsero de Medellín. Conectamos bailarines con las mejores clases,
			eventos y productos de la escena. Desde el horario semanal de clases hasta nuestra tienda
			con el estilo de la rumba colombiana.
		</p>
		<p class="about-text-en">
			Medellín's salsa ecosystem. Classes, events, and gear for dancers.
		</p>
	</div>
</section>

<style>
	.about {
		padding: var(--space-lg) var(--space-sm);
		text-align: center;
	}

	.about-heading {
		font-family: var(--font-display);
		font-size: 2rem;
		color: var(--zouk);
		margin-bottom: var(--space-sm);
	}

	.about-text {
		max-width: 48ch;
		margin-inline: auto;
		font-size: 1.05rem;
		line-height: 1.6;
		color: var(--text);
	}

	.about-text-en {
		max-width: 48ch;
		margin-inline: auto;
		margin-top: var(--space-sm);
		font-size: 0.9rem;
		color: var(--text-muted);
		font-style: italic;
	}
</style>
